# Parametrix brand assets

## Status

The Parametrix logo was supplied in conversation on 2026-09-10 as two marks:

1. **Wordmark** — "Parametrix" set in a heavy geometric sans, dark charcoal,
   with a red square serving as the tittle above the "i".
2. **X mark** — the wordmark's terminal "X" used standalone, with a red square
   set at the upper left.

**The image files did not transfer to this environment** and are not stored here.
To embed the logo in the SOP, commit the source files to this directory —
preferably vector (SVG or EPS) so they stay sharp in print and at any size.

Suggested filenames:

- `parametrix-wordmark.svg`
- `parametrix-x-mark.svg`

## Colors

Approximate values read from the supplied images. **Confirm against the
Parametrix brand standard before publication** — these are sampled, not official.

| Role | Approx. hex |
|---|---|
| Charcoal (primary text/mark) | `#333333` |
| Red (accent square) | `#EE2B2B` |

## PARAMETRIX DECISION REQUIRED

Confirm the official brand palette, approved logo lockups, minimum clear space,
and minimum reproduction size, and confirm whether the wordmark or the X mark is
correct for an internal SOP cover.
